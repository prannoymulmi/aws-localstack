{
  "name": "src",
  "version": "1.0.0",
  "description": "Prototype for OIDC PKCE implementation",
  "main": "handler.js",
  "author": "Prannoy Mulmi",
  "license": "MIT",
  "scripts": {
    "build": "./node_modules/.bin/tsc",
    "build:zip": "npm run build && npx tsc --project ./tsconfig.json && zip -r dist/lambda_function.zip dist node_modules",
    "clean": "rm -rf dist && rm -rf node_modules && rm -rf deployment",
    "build:esbuild": "node build.mjs",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "deploy": "npm run build:zip && npm run build:esbuild"
  },
  "dependencies": {
    "argon2": "^0.41.1",
    "esbuild": "^0.24.0",
    "hash-wasm": "^4.11.0",
    "jsonwebtoken": "^9.0.2",
    "uuidv4": "^6.2.13",
    "winston": "^3.15.0",
    "winston-cloudwatch": "^6.3.0"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.675.0",
    "@types/aws-lambda": "^8.10.145",
    "@types/jest": "^29.5.14",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^22.7.7",
    "aws-sdk-client-mock": "^4.1.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  }
}
