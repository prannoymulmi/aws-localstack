{
  "name": "src",
  "version": "1.0.0",
  "description": "Prototype for OIDC PKCE implementation",
  "main": "handler.js",
  "author": "Prannoy Mulmi",
  "license": "MIT",
  "scripts": {
    "build": "./node_modules/.bin/tsc",
    "deploy": "npx tsc --project ./tsconfig.json && zip -r dist/lambda_function.zip dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.675.0",
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.7.7",
    "typescript": "^5.6.3"
  }
}
